# ============================================================
# TeamCity Model Comparison
# ============================================================

# BuildQueueRequest ? BuildQueueResponse
BuildQueueRequest=BuildQueueResponse:buildTypeId=buildTypeId,branchName=branchName

# BuildRequest ? BuildResponse
BuildRequest=BuildResponse:buildTypeId=buildTypeId,branchName=branchName

# BuildTagsRequest ? BuildTagsResponse
BuildTagsRequest=BuildTagsResponse:tags=tags

# DepositRequest ? DepositResponse (?? ???????)
DepositRequest=DepositResponse:id=id

# TransferRequest ? TransferResponse (?? ???????)
TransferRequest=TransferResponse:senderAccountId=senderAccountId,receiverAccountId=receiverAccountId,amount=amount

# UpdateCustomerProfileRequest ? UpdateCustomerProfileResponse (?? ???????)
UpdateCustomerProfileRequest=UpdateCustomerProfileResponse:name=customer.name

# ============================================================
# ???????????? ???? (?? ??????????)
# ============================================================

# BuildQueueResponse ? BuildQueueResponse.ignore
BuildQueueResponse=BuildQueueResponse.ignore:id,href,webUrl,state,status

# BuildResponse ? BuildResponse.ignore
BuildResponse=BuildResponse.ignore:id,number,startDate,finishDate